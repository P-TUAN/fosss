@model IEnumerable<MVCBookStore.Models.NHAXUATBAN>
<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SPTheoNXB","BookStore",new { id=item.MaNXB})" class="list-group-item" style="background-color:#087b39; color:white; text-align:center;font-weight:bold">
            @item.TenNXB
        </a>
    }
</div>
