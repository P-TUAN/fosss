@model IEnumerable<MVCBookStore.Models.CHUDE>
<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SPTheochude","BookStore",new { id =item.MaCD})" class="list-group-item" style="background-color:#087b39; color:white; text-align:center;font-weight:bold">
            @item.TenChuDe
        </a>
    }
</div>